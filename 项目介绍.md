# 膳语 — 项目介绍

> AI 驱动的智能食材管理与食谱生成平台

---

## 一、项目概述

**膳语**是一款面向家庭用户的智能厨房助手，核心解决三个痛点：

1. **食材浪费** — 冰箱食材过期被遗忘，中国每年家庭食物浪费超 3500 万吨
2. **选择困难** — 面对零散食材不知道做什么，重复做同样的菜
3. **记录繁琐** — 传统记账式食材管理费时费力，难以坚持

**核心价值**：以"临期优先、减少浪费"为理念，打通"食材识别 → 智能管理 → AI 食谱 → 购物补货"全链路。

---

## 二、技术栈

### 前端
| 技术 | 用途 |
|------|------|
| **uni-app** | 跨端开发框架，一套代码输出 H5 + App |
| **Vue 3 + TypeScript** | 组件化开发，类型安全 |
| **Pinia** | 全局状态管理 |
| **SCSS** | 样式预处理，Material Design 3 设计体系 |

### 后端
| 技术 | 用途 |
|------|------|
| **Fastify** | 高性能 Node.js Web 框架 |
| **TypeScript** | 全栈类型安全 |
| **Prisma ORM** | 数据库操作与迁移管理 |
| **SQLite** | 轻量级关系型数据库，单文件部署 |
| **JWT** | 用户身份认证（accessToken + refreshToken 双 Token） |
| **bcryptjs** | 密码哈希（验证码、Token 摘要） |
| **nodemailer** | QQ SMTP 邮件服务发送登录验证码 |

### AI 能力
| 服务 | 用途 |
|------|------|
| **阿里云 OCR** | 购物小票文字识别 |
| **智谱 OCR (glm-ocr)** | 小票识别备用方案 |
| **Moonshot (glm-4.5-air)** | 大语言模型生成个性化食谱 |
| **结构化 Prompt 工程** | 约束模型输出标准 JSON 格式的食谱数据 |

### 部署运维
| 技术 | 用途 |
|------|------|
| **腾讯云轻量服务器** | 公网部署 |
| **宝塔面板** | 可视化运维、Nginx 配置 |
| **pm2** | Node.js 进程守护与开机自启 |
| **nginx** | 反向代理、负载均衡 |

---

## 三、已实现功能

### 1. 用户系统
- **邮箱验证码登录**：输入邮箱 → QQ SMTP 发送 6 位验证码 → 验证登录
- **访客登录**：一键生成临时账号，零门槛体验
- **Token 自动刷新**：accessToken 过期后自动用 refreshToken 续期
- **用户信息拉取**：登录后自动同步昵称等基本信息

### 2. 智能食材库
- **手动录入**：分类添加蔬菜、肉类、调料等食材
- **OCR 自动入库**：拍照识别超市购物小票，自动提取：
  - 食材名称（智能归一化为标准名称）
  - 重量/数量（自动转换为克/个）
  - 保质期（基于食材类型智能估算：叶菜 3 天、肉类 2 天、调料 365 天等）
  - 存储方式（冷藏/冷冻/常温）
- **临期追踪**：可视化展示食材剩余保质期，临期高亮提醒

### 3. AI 食谱生成
- **多模式触发**：支持快捷指令（如"临期快手菜""减脂餐""清冰箱"）
- **上下文感知**：AI 知道你的库存食材、临期状态、可用厨具
- **结构化输出**：返回标准 JSON，包含：
  - 菜品名称、描述、难度、耗时
  - 营养信息（热量、蛋白质）
  - 食材消耗计划（匹配库存）
  - 步骤清单（含时间和技巧）
  - 购物清单（缺料自动列出）
- **数据库持久化**：生成的食谱自动保存，支持历史回看

### 4. 购物清单
- **食谱联动**：生成食谱后自动比对库存，列出缺料
- **手动添加**：自由添加待购物品
- **勾选状态**：已购/未购标记

### 5. 系统特性
- **Material Design 3 设计**：圆角卡片、渐变色彩、流畅动画
- **暗色模式**：自动/手动切换，全应用适配
- **响应式布局**：手机端 + 桌面端自适应
- **数据加密存储**：本地敏感数据 AES 加密（H5 端）

---

## 四、创新点

| 创新点 | 说明 |
|--------|------|
| **临期优先算法** | 不同于传统菜谱 App 的"人找菜"，膳语主动推荐消耗临期食材的方案 |
| **小票自动入库** | OCR 拍照即可建立数字化食材档案，大幅降低记录门槛 |
| **意图理解生成** | 支持自然语言（"15 分钟快手菜"），大模型自动匹配库存生成方案 |
| **全链路闭环** | 食材管理 → 食谱规划 → 购物补货，覆盖家庭烹饪完整场景 |

---

## 五、项目结构

```
last_shipu/
├── backend/                  # 后端服务
│   ├── src/
│   │   ├── lib/             # 工具库（auth、db、env、mail、response）
│   │   ├── routes/          # API 路由（auth、inventory、pantry、recipes、ocr...）
│   │   ├── providers/       # 第三方服务（zhipu/llm、zhipu/ocr）
│   │   ├── seed/            # 初始数据（食材库）
│   │   └── types/           # TypeScript 类型声明
│   ├── prisma/
│   │   ├── schema.prisma    # 数据库模型定义
│   │   └── migrations/      # 数据库迁移文件
│   └── .env                 # 环境变量配置
├── frontend/app/            # 前端 uni-app 项目
│   ├── src/
│   │   ├── pages/           # 页面（home、login、aiCreate、ingredients...）
│   │   ├── stores/          # Pinia 状态管理
│   │   ├── api/             # HTTP 请求封装
│   │   ├── utils/           # 工具函数
│   │   └── static/          # 静态资源
│   └── manifest.json        # App 配置
├── deploy/                  # 部署脚本
│   ├── deploy.sh            # 后端一键部署脚本
│   └── nginx-bt.conf        # 宝塔面板 nginx 配置
└── 答辩PPT.html              # 答辩演示文稿
```

---

## 六、部署状态

| 环境 | 状态 | 地址 |
|------|------|------|
| 本地开发 | ✅ 运行中 | http://localhost:5173 (H5) |
| 服务器后端 | ✅ 已部署 | http://shanyushipu.top:8787 |
| App 端 | ⚠️ 适配中 | uni-app 运行时兼容问题修复中 |

---

## 七、未来规划

- **短期**：修复 App 端兼容性，完成多端测试
- **中期**：接入食材价格追踪、优惠券分发
- **长期**：社区食谱分享、智能冰箱 IoT 联动、营养师审核机制

---

> **汇报人**：陈絮  
> **专业**：人工智能 大三  
> **指导教师**：陈璞花  
> **项目类型**：创业训练项目
